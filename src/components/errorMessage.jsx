import React from 'react'

const ErrorMessage = () => {
  return (
    <span className="error-message-block">
      Task name should not be empty and should be unique
    </span>
  )
}

export default ErrorMessage
